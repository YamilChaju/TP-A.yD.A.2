/*DameJugada()
{
    if ((LetraO==LetraU)&&(FilaO==FilaU))
        if(MovimientoValido(LetraO,FilaO,LetraO,FilaO-1,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO-1,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO,Jugador,actual)==true)
                    return jugada;
                else
                    if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO+1,Jugador,actual)==true)
                        return jugada;
                    else
                        if(MovimientoValido(LetraO,FilaO,LetraO,FilaO+1,Jugador,actual)==true)
                            return jugada;
                        else
                            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
                                return jugada;
                            else
                                if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                                    return jugada;
                                else
                                    if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                                        return jugada;
                                    else nohaymovimiento;
if (LetraO==LetraU)&&(FilaO>FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraO,FilaU-1,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO-1,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO+1,Jugador,actual)==true)
                    return jugada;
                else
                    if(MovimientoValido(LetraO,FilaO,LetraO,FilaO+1,Jugador,actual)==true)
                        return jugada;
                    else
                        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
                            return jugada;
                        else
                            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                                return jugada;
                            else
                                if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                                    return jugada;
                                else
                                    nohaymovimient;
if (LetraO<LetraU)&&(FilaO>FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU+1,FilaU-1,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO+1,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO,FilaO+1,Jugador,actual)==true)
                    return jugada;
                else
                    if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
                        return jugada;
                    else
                        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                            return jugada;
                        else
                            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                                return jugada;
                            else
                                return nohayjugada;
if (LetraO<LetraU)&&(FilaO==FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU+1,FilaO,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO+1,FilaO+1,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO,FilaO+1,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
                    return jugada;
                else
                    if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                        return jugada;
                    else
                        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                            return jugada;
                        else
                            retunr nohayjugada;
if (LetraO<LetraU)&&(FilaO<FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU+1,FilaU+1,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO,FilaO+1,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                    return jugada;
                else
                    if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                        return jugada;
                    else
                        noahyjugada;
if (LetraO==LetraU)&&(FilaO<FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraO,FilaU+1,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO+1,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
                return jugada;
            else
                if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                    return jugada;
                else
                    return nohayjugada;
if (LetraO>LetraU)&&(FilaO<FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU-1,FilaU+1,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO,Jugador,actual)==true)
            return jugada;
        else
            if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
                return jugada;
            else
                return nohayjugada;
if (LetraO>LetraU)&&(FilaO==FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU-1,FilaO,Jugador,actual)==true)
        return jugada;
    else
        if(MovimientoValido(LetraO,FilaO,LetraO-1,FilaO-1,Jugador,actual)==true)
            return jugada;
        else
            return nohayjugada;
if (LetraO>LetraU)&&(FilaO>FilaU)
    if(MovimientoValido(LetraO,FilaO,LetraU-1,FilaU-1,Jugador,actual)==true)
        return jugada;
    else
        return nohayjugada;
}*/